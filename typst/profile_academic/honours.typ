#import "../lib.typ": *

// Honours use the compact `one-line` helper — bold title left, italic
// muted metadata right. Each honour is a list item with minimal data
// (name + amount + location + year), so a 2-line format would be bloat.

#section("Honours & Grants")

#subsection("Fellowships & Awards")

#one-line(
  [CONACYT Graduate Fellowship #amount[(≈19K CAD)]],
  [Ciudad de México, México · 2017–2019],
)

#one-line(
  [IPN Entrance Award #amount[(≈6K CAD)]],
  [Montréal, QC, Canada · 2020],
)

#one-line(
  [CONACYT Mobility Fellowship #amount[(≈1.9K CAD)]],
  [Ciudad de México, México · 2019],
)

#one-line(
  [UNAM Support for Mobility #amount[(≈1.3K CAD)]],
  [Ciudad de México, México · 2019],
)

#subsection("Recognitions")

#one-line(
  ["Alfonso Caso" Medal],
  [Querétaro, México · 2020],
)

#one-line(
  [EGEL-CENEVAL Excellence Award],
  [Guadalajara, Jal., México · 2016],
)
